import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Create Installer Profile',
  description: 'Register your installer profile on WrapCareers to be discovered by automotive employers.',
};

export default function InstallerRegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
